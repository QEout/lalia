import 'package:flutter/material.dart';

/// 主题
class laliaTheme {
  static ThemeData goldTheme() {
    return ThemeData(
        primaryColor: Color.fromRGBO(225, 193, 150, 1),
        primarySwatch: Colors.orange,
        floatingActionButtonTheme: FloatingActionButtonThemeData(
            foregroundColor: Colors.white
        ),
        appBarTheme: AppBarTheme(
            brightness: Brightness.light,
            iconTheme: IconThemeData(color: Colors.black),
            elevation: 0,
            color: Color.fromRGBO(239, 238, 238, 1.0),
            textTheme: TextTheme(title: TextStyle(color: Colors.black))
        ),
        cardColor: Colors.grey[300],
        bottomAppBarColor: Color.fromRGBO(239, 238, 238, 1.0),
        scaffoldBackgroundColor: Color.fromRGBO(239, 238, 238, 1.0),
        indicatorColor: Color.fromRGBO(225, 193, 150, 1),
        cursorColor: Color.fromRGBO(225, 193, 150, 1),
        inputDecorationTheme: InputDecorationTheme(
            fillColor: Colors.grey[200]
        )
    );
  }

  static ThemeData blueTheme() {
    return ThemeData(
        primaryColor: Colors.blue,
        primarySwatch: Colors.blue,
        floatingActionButtonTheme: FloatingActionButtonThemeData(
            foregroundColor: Colors.white
        ),
        appBarTheme: AppBarTheme(
            brightness: Brightness.light,
            iconTheme: IconThemeData(color: Colors.black),
            elevation: 0,
            color: Color.fromRGBO(239, 238, 238, 1.0),
            textTheme: TextTheme(title: TextStyle(color: Colors.black))
        ),
        cardColor: Colors.grey[300],
        bottomAppBarColor: Color.fromRGBO(239, 238, 238, 1.0),
        scaffoldBackgroundColor: Color.fromRGBO(239, 238, 238, 1.0),
        indicatorColor: Colors.blue,
        cursorColor: Colors.blue,
        inputDecorationTheme: InputDecorationTheme(
            fillColor: Colors.grey[200]
        )
    );
  }

  static ThemeData redTheme() {
    return ThemeData(
        primaryColor: Colors.red,
        primarySwatch: Colors.red,
        floatingActionButtonTheme: FloatingActionButtonThemeData(
            foregroundColor: Colors.white
        ),
        appBarTheme: AppBarTheme(
            brightness: Brightness.light,
            iconTheme: IconThemeData(color: Colors.black),
            elevation: 0,
            color: Color.fromRGBO(239, 238, 238, 1.0),
            textTheme: TextTheme(title: TextStyle(color: Colors.black))
        ),
        bottomAppBarColor: Color.fromRGBO(239, 238, 238, 1.0),
        scaffoldBackgroundColor: Color.fromRGBO(239, 238, 238, 1.0),
        indicatorColor: Colors.red,
        cursorColor: Colors.red,
        inputDecorationTheme: InputDecorationTheme(
            fillColor: Colors.grey[200]
        )
    );
  }

  static ThemeData tealTheme() {
    return ThemeData(
        primaryColor: Colors.teal,
        primarySwatch: Colors.teal,
        floatingActionButtonTheme: FloatingActionButtonThemeData(
            foregroundColor: Colors.white
        ),
        appBarTheme: AppBarTheme(
            brightness: Brightness.light,
            iconTheme: IconThemeData(color: Colors.black),
            elevation: 0,
            color: Color.fromRGBO(239, 238, 238, 1.0),
            textTheme: TextTheme(title: TextStyle(color: Colors.black))
        ),
        bottomAppBarColor: Color.fromRGBO(239, 238, 238, 1.0),
        scaffoldBackgroundColor: Color.fromRGBO(239, 238, 238, 1.0),
        indicatorColor: Colors.teal,
        cursorColor: Colors.teal,
        inputDecorationTheme: InputDecorationTheme(
            fillColor: Colors.grey[200]
        )
    );
  }

  static ThemeData deepPurpleTheme() {
    return ThemeData(
        primaryColor: Colors.deepPurple,
        primarySwatch: Colors.deepPurple,
        floatingActionButtonTheme: FloatingActionButtonThemeData(
            foregroundColor: Colors.white
        ),
        appBarTheme: AppBarTheme(
            brightness: Brightness.light,
            iconTheme: IconThemeData(color: Colors.black),
            elevation: 0,
            color: Color.fromRGBO(239, 238, 238, 1.0),
            textTheme: TextTheme(title: TextStyle(color: Colors.black))
        ),
        bottomAppBarColor: Color.fromRGBO(239, 238, 238, 1.0),
        scaffoldBackgroundColor: Color.fromRGBO(239, 238, 238, 1.0),
        indicatorColor: Colors.deepPurple,
        cursorColor: Colors.deepPurple,
        inputDecorationTheme: InputDecorationTheme(
            fillColor: Colors.grey[200]
        )
    );
  }

  static ThemeData orangeTheme() {
    return ThemeData(
        primaryColor: Colors.orange,
        primarySwatch: Colors.orange,
        floatingActionButtonTheme: FloatingActionButtonThemeData(
            foregroundColor: Colors.white
        ),
        appBarTheme: AppBarTheme(
            brightness: Brightness.light,
            iconTheme: IconThemeData(color: Colors.black),
            elevation: 0,
            color: Color.fromRGBO(239, 238, 238, 1.0),
            textTheme: TextTheme(title: TextStyle(color: Colors.black))
        ),
        bottomAppBarColor: Color.fromRGBO(239, 238, 238, 1.0),
        scaffoldBackgroundColor: Color.fromRGBO(239, 238, 238, 1.0),
        indicatorColor: Colors.orange,
        cursorColor: Colors.orange,
        inputDecorationTheme: InputDecorationTheme(
            fillColor: Colors.grey[200]
        )
    );
  }

  static ThemeData pinkTheme() {
    return ThemeData(
        primaryColor: Colors.pink,
        primarySwatch: Colors.pink,
        floatingActionButtonTheme: FloatingActionButtonThemeData(
            foregroundColor: Colors.white
        ),
        appBarTheme: AppBarTheme(
            brightness: Brightness.light,
            iconTheme: IconThemeData(color: Colors.black),
            elevation: 0,
            color: Color.fromRGBO(239, 238, 238, 1.0),
            textTheme: TextTheme(title: TextStyle(color: Colors.black))
        ),
        bottomAppBarColor: Color.fromRGBO(239, 238, 238, 1.0),
        scaffoldBackgroundColor: Color.fromRGBO(239, 238, 238, 1.0),
        indicatorColor: Colors.pink,
        cursorColor: Colors.pink,
        inputDecorationTheme: InputDecorationTheme(
            fillColor: Colors.grey[200]
        )
    );
  }

  static ThemeData blueGreyTheme() {
    return ThemeData(
        primaryColor: Colors.blueGrey,
        primarySwatch: Colors.blueGrey,
        floatingActionButtonTheme: FloatingActionButtonThemeData(
            foregroundColor: Colors.white
        ),
        appBarTheme: AppBarTheme(
            brightness: Brightness.light,
            iconTheme: IconThemeData(color: Colors.black),
            elevation: 0,
            color: Color.fromRGBO(239, 238, 238, 1.0),
            textTheme: TextTheme(title: TextStyle(color: Colors.black))
        ),
        cardColor: Colors.grey[300],
        bottomAppBarColor: Color.fromRGBO(239, 238, 238, 1.0),
        scaffoldBackgroundColor: Color.fromRGBO(239, 238, 238, 1.0),
        indicatorColor: Colors.blueGrey,
        cursorColor: Colors.blueGrey,
        inputDecorationTheme: InputDecorationTheme(
            fillColor: Colors.grey[200]
        )
    );
  }

  static ThemeData darkTheme() {
    return ThemeData(
        primaryColor: Colors.blueAccent,
        primarySwatch: Colors.blue,
        floatingActionButtonTheme: FloatingActionButtonThemeData(
            foregroundColor: Colors.white
        ),
        appBarTheme: AppBarTheme(
          brightness: Brightness.dark,
          iconTheme: IconThemeData(color: Colors.white70),
          elevation: 0,
          color: Colors.grey[900],
        ),
        iconTheme: IconThemeData(
            color: Colors.grey[200]
        ),
        scaffoldBackgroundColor: Colors.grey[900],
        cardColor: Colors.grey[400],
        indicatorColor: Colors.blueAccent,
        cursorColor: Colors.blueAccent,
        accentColor: Colors.blueAccent,
        textTheme: TextTheme(
          title: TextStyle(color: Colors.grey),
          display1: TextStyle(color: Colors.grey),
          display2: TextStyle(color: Colors.grey),
          display3: TextStyle(color: Colors.grey),
          display4: TextStyle(color: Colors.grey),
          caption: TextStyle(color: Colors.grey),
          body1: TextStyle(color: Colors.grey),
          body2: TextStyle(color: Colors.grey),
          subtitle: TextStyle(color: Colors.grey),
          subhead: TextStyle(color: Colors.grey),
          button: TextStyle(color: Colors.grey),
          headline: TextStyle(color: Colors.grey),
        ),
        bottomAppBarColor: Colors.grey[900],
        dialogTheme: DialogTheme(
          backgroundColor: Colors.grey[900],
        ),
        bottomSheetTheme: BottomSheetThemeData(
            modalBackgroundColor: Colors.grey[900]
        ),
        dividerColor: Colors.grey,
        inputDecorationTheme: InputDecorationTheme(
          labelStyle: TextStyle(color: Colors.white),
          fillColor: Colors.black,
          hintStyle: TextStyle(color: Colors.grey),
        ),
        buttonTheme: ButtonThemeData(
            textTheme: ButtonTextTheme.accent
        ),
        chipTheme: ChipThemeData(
          backgroundColor: Colors.grey[700],
          brightness: Brightness.dark,
          disabledColor: Colors.grey[700],
          labelPadding: EdgeInsets.symmetric(horizontal: 8, vertical: 0),
          labelStyle: TextStyle(color: Colors.white),
          padding: EdgeInsets.all(4),
          secondaryLabelStyle: TextStyle(color: Colors.white),
          secondarySelectedColor: Colors.blue,
          selectedColor: Colors.blue,
          shape: StadiumBorder(),
        ),
        canvasColor: Colors.black
    );
  }
}